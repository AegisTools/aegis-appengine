digraph G {

  tags -> users;

  projects -> users;
  projects -> tags;

  tasks -> users;
  tasks -> tags;

  assignments -> users;
  assignments -> projects;
  assignments -> tasks;
  assignments -> tags;

  timesheets -> users;
  timesheets -> tags;
  timesheets -> assignments;

  issues -> users;
  issues -> tags;
}
