digraph G {

  tags -> users;

  clients -> users;
  clients -> tags;

  projects -> users;
  projects -> tags;
  projects -> clients;

  tasks -> users;
  tasks -> tags;

  assignments -> users;
  assignments -> projects;
  assignments -> tasks;
  assignments -> tags;

  timesheets -> users;
  timesheets -> tags;
  timesheets -> assignments;

  issues -> users;
  issues -> tags;
  issues -> projects;
}
